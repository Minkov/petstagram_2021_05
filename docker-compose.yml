
version: '3.4'

services:
  postgres:
    container_name: petstagram_instance
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: petstagram
      POSTGRES_USER: db_admin
      POSTGRES_PASSWORD: 1123QwER
    volumes:
      - ./postgresql/data:/var/lib/postgresql/data
